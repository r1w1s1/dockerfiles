version: '3'

services:
  caddy-php71:
    build:
      context: .
    image: caddy-php71 
    ports:
      - "80:80"
      - "443:443"
    #volumes:
    #  - /srv/docker/apache2/prod:/var/www/localhost/htdocs

